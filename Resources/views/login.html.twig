{% if error  %}
<div>
  <div class="ui-widget">
    <div style="padding: 10px;margin-top: 20px; margin-bottom: 20px" class="ui-state-error ui-corner-all">
        <p><span style="float: left; margin-right: .3em;" class="ui-icon ui-icon-alert"></span>
        <strong>Alerta:</strong> {{ error.message|trans({},'maspyma-usuarios-messages') }} </p>
    </div>
    </div>
</div>
{% endif %}
{#{% if error or (app.user is not null) %}
<div>
  <div class="ui-widget">
    <div style="padding: 10px;margin-top: 20px; margin-bottom: 20px" class="ui-state-error ui-corner-all">
        <p><span style="float: left; margin-right: .3em;" class="ui-icon ui-icon-alert"></span>
        <strong>Alerta:</strong> No posee los permisos necesarios para ingresar a esta sección. </p>
    </div>
    </div>
</div>
{% endif %}#}
<div class="row form">
    <h3>Ingreso:</h3>
    <form action="{{ path('login_check') }}" method="post" novalidate="novalidate">
        <div class="row">
            <div class="fourcol">
                <label for="username">Usuario:</label>
                <input class="input" type="text" id="username" name="_username" value="{{ last_username }}" required="required" />
                <label for="password">Contraseña:</label>
                <input class="input" type="password" id="password" name="_password" required="required" />
                {#
                If you want to control the URL the user
                is redirected to on success (more details below)
                <input type="hidden" name="_target_path" value="/account" />
                #}
                
                <div class="row">                    
                    <button type="submit">Aceptar</button>
                </div>
            </div>
        </div>
    </form>
</div>